# StitchFlow Desktop — dépendances Python
# pip install -r requirements.txt

# Core Django
django>=6.0
python-decouple>=3.8
dj-database-url>=2.0
django-vite>=3.0
django-ratelimit>=4.1
whitenoise>=6.6

# Broderie
pyembroidery>=1.5

# Images et vectorisation
pillow>=10.0
numpy>=1.24
vtracer>=0.6
pdf2image>=1.17

# Sécurité
defusedxml>=0.7

# rembg>=2.0  # optionnel — suppression fond IA (fallback Pillow si absent)
